services:
  spring-student03-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/spring-student03-app:latest
    container_name: spring-student03-app
    ports:
      - ${APP_PORT:-9003}:${APP_PORT:-9003}
    env_file:
      - .env
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:$${APP_PORT:-9003}/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - classroom_classroom_net

networks:
  classroom_classroom_net:
    external: true
